# Планировщик сеансов связи со спутниками v4

Учебная Python-система для планирования связи между LEO-спутниками и наземными станциями. Программа берёт реальные TLE-данные, рассчитывает видимость спутника над станцией, фильтрует пролёты, оценивает качество связи, решает конфликты расписания, считает передачу данных и формирует отчёты.

## Что нового в v4

Реализованы дополнительные улучшения:

1. **SQLite-база данных** — история расчётов, сеансы, станции и спутники сохраняются в `history.sqlite`.
2. **Сравнение сценариев** — можно сравнить несколько пресетов по числу сеансов, минутам связи, переданным МБ и среднему индексу качества.
3. **Рекомендованное расписание** — программа может выбрать все сеансы, топ-N, лучший сеанс на спутник или только сеансы не ниже класса A/B/C.
4. **Диаграмма загрузки станций** — PNG-график показывает, когда каждая станция занята.
5. **Контроль устаревания TLE** — программа предупреждает, если TLE старше 7 или 14 дней.
6. **Автоматическое обновление TLE** — можно скачать TLE в локальный кэш.
7. **Карта покрытия станции** — на интерактивной карте показаны станции, трассы спутников и приближённые зоны видимости.
8. **Индекс качества 0-100** — вместо абстрактных баллов теперь есть понятный индекс качества и класс A/B/C.
9. **Погодные ограничения** — условная погода станции влияет на индекс качества; гроза запрещает сеансы.
10. **PDF-отчёт** — кроме HTML, CSV, JSON и ICS создаётся `report.pdf`. В версии v5 исправлена проблема с кириллицей: PDF использует Unicode-шрифт, поэтому русские буквы не превращаются в чёрные квадраты.
11. **Страница “Объяснение” в интерфейсе** — отдельная вкладка объясняет алгоритм простым языком.
12. **Расширенная проверка входных данных** — координаты, длительности, шаги, углы, скорости и погода проверяются до расчёта.

## Установка

```bash
python -m venv .venv
source .venv/bin/activate        # Linux/macOS
# .venv\Scripts\activate         # Windows

pip install -r requirements.txt
```

## Запуск веб-интерфейса

```bash
streamlit run web_app.py
```

В интерфейсе есть вкладки:

- **Расчёт** — выбор пресета, спутников, станций, погоды, фильтров и генерация отчётов;
- **Сравнение сценариев** — сравнение нескольких готовых систем;
- **История** — просмотр последних расчётов из SQLite;
- **Объяснение** — краткая теория и описание алгоритма.

## Исправления v7

- Пресет `noaa_weather` теперь использует прямой запрос CelesTrak по NORAD ID `33591` для NOAA 19. Это исправляет ошибку, когда групповой список `weather` не содержит NOAA 19.
- Поиск спутника стал устойчивее: `NOAA 19`, `NOAA-19` и `33591` корректно сопоставляются с одной TLE-записью, если она есть в источнике.
- Диаграмма загрузки станций переделана: короткие LEO-сеансы теперь показаны крупными маркерами, а не почти невидимыми тонкими полосками. Подписи больше не накладываются на каждое событие.


## Быстрый запуск CLI

```bash
python session_planner.py --preset iss_moscow
```

После запуска результаты появятся в папке `outputs/iss_moscow/`:

- `schedule.csv` — таблица с русскими заголовками;
- `schedule.json` — полные данные с профилями пролётов;
- `report.html` — автономный HTML-отчёт с картой, графиками и таблицей;
- `report.pdf` — PDF-отчёт;
- `map.html` — отдельная интерактивная карта;
- `station_utilization.png` — диаграмма загрузки станции;
- `schedule.ics` — календарь;
- `history.sqlite` — база истории расчётов.

## Готовые сценарии

```bash
python session_planner.py --list-presets
```

Доступны:

- `iss_moscow` — МКС и одна станция в Москве;
- `iss_multi_station` — МКС и несколько наземных станций;
- `noaa_weather` — метеоспутник NOAA 19;
- `night_passes` — только ночные пролёты;
- `station_group_multi_sat` — несколько спутников и конфликты станции;
- `weather_limited` — демонстрация погодных ограничений.

## Сравнение сценариев

```bash
python session_planner.py \
  --compare-presets iss_moscow,iss_multi_station,weather_limited \
  --out-dir outputs/comparison
```

Будут созданы:

- `outputs/comparison/scenario_comparison.csv`;
- `outputs/comparison/scenario_comparison.html`.

## Рекомендованное расписание

Примеры:

```bash
# взять топ-5 лучших сеансов
python session_planner.py --preset iss_moscow --recommend-mode top_n --max-recommended 5

# оставить рекомендованными только сеансы класса A и B
python session_planner.py --preset iss_moscow --recommend-mode quality --min-recommend-class B

# выбрать лучший сеанс для каждого спутника
python session_planner.py --preset station_group_multi_sat --recommend-mode best_per_satellite
```

В таблице есть колонка **Рекомендован**.

## Обновление TLE

```bash
python session_planner.py \
  --tle "https://celestrak.org/NORAD/elements/gp.php?GROUP=stations&FORMAT=tle" \
  --update-tle \
  --tle-cache data/stations_latest.tle \
  --preset iss_moscow
```

Можно затем запускать из кэша:

```bash
python session_planner.py --preset iss_moscow --use-tle-cache --tle-cache data/stations_latest.tle
```

## История расчётов SQLite

Расчёт сохраняется в базу автоматически, если задан путь `outputs.db` в конфиге или аргумент `--db`:

```bash
python session_planner.py --preset iss_moscow --db outputs/history.sqlite
python session_planner.py --list-runs --db outputs/history.sqlite
```

## Погодные ограничения

Погода задаётся для станции:

```yaml
stations:
  - name: Moscow GS
    lat: 55.7539
    lon: 37.6208
    min_elevation_deg: 10
    weather: rain
```

Поддерживаются значения:

```text
clear, cloudy, rain, snow, storm, fog
```

Плохая погода снижает индекс качества, а `storm` запрещает сеансы.

## Пример YAML-конфига

```yaml
tle: examples/iss_sample.tle
satellites:
  - name: ISS
stations:
  - name: Moscow GS
    lat: 55.7539
    lon: 37.6208
    elevation_m: 150
    min_elevation_deg: 10
    weather: clear
planning:
  start: now
  hours: 48
  coarse_step_sec: 60
  sample_step_sec: 10
  resolve_conflicts: true
filters:
  min_duration_min: 5
  min_max_elevation_deg: 15
data:
  downlink_mbps: 2
  capacity_mb_per_pass: 500
  data_generation_rate_mbps: 0.2
  initial_backlog_mb: 0
recommendation:
  mode: top_n
  max_count: 5
  min_quality_class: B
outputs:
  out_dir: outputs
  html: outputs/report.html
  pdf: outputs/report.pdf
  db: outputs/history.sqlite
```

## Как программа работает кратко

1. Загружает TLE спутников.
2. Проверяет возраст TLE и входные параметры.
3. На заданном интервале времени считает положение спутника относительно станции.
4. Находит окна, где спутник выше минимального угла места.
5. Считает длительность, максимальный угол, средний угол, дальность и день/ночь.
6. Учитывает условную погоду и считает индекс качества 0-100.
7. Убирает конфликтующие сеансы одной станции.
8. Выбирает рекомендованное расписание.
9. Считает, сколько данных можно передать.
10. Сохраняет таблицы, отчёты, карту, графики и историю.

## Структура проекта

```text
sat_session_planner_v9/
├── session_planner.py
├── web_app.py
├── requirements.txt
├── examples/
│   ├── config.yaml
│   ├── iss_sample.tle
│   ├── horizon_mask.csv
│   ├── stations.csv
│   └── presets/
├── satplan/
│   ├── cli.py
│   ├── config.py
│   ├── database.py
│   ├── horizon.py
│   ├── models.py
│   ├── outputs.py
│   ├── planning.py
│   ├── presets.py
│   ├── scenario.py
│   ├── sun.py
│   ├── tle.py
│   ├── utils.py
│   ├── validation.py
│   └── web_app.py
└── tests/
```

## Примечание

Это учебная система. Для настоящей эксплуатации нужно дополнительно учитывать частоты, мощность передатчика, диаграммы антенн, реальные погодные данные, правила радиосвязи и более строгую валидацию орбитальных данных.

## Исправления v6: проверка визуализаций

В версии v6 дополнительно проверены и исправлены ошибки вывода:

- интерактивная карта теперь разбивает треки при пересечении 180-го меридиана, поэтому линия не тянется через всю карту;
- добавлена статическая PNG-карта `static_map.png`, которая встраивается в HTML и PDF как запасной вариант, если Folium/Leaflet не загрузится;
- PDF-отчёт теперь включает не только таблицу, но и карту, диаграмму загрузки станций и первые графики угла места;
- диаграмма загрузки станций исправлена: подписи больше не залезают на заголовок;
- графики угла места получили более понятные оси времени UTC и отметку максимального угла;
- исправлено дублирование `SUMMARY` в `.ics`-файле;
- добавлены тесты для HTML/PDF/PNG/ICS-выводов и для треков через 180-й меридиан.

Проверка проекта:

```bash
python -m compileall -q .
pytest -q
```

Ожидаемый результат: все тесты проходят успешно.

## Исправления v9: нормальная карта в PDF

В PDF нельзя встроить интерактивную Leaflet/Folium-карту, потому что PDF не выполняет JavaScript. Поэтому программа создаёт отдельную статическую PNG-карту и вставляет её в PDF.

В версии v9 эта статическая карта исправлена:

- вместо пустой координатной сетки теперь рисуется географическая подложка с материками и береговыми линиями;
- если в системе установлен `Basemap`, используются реальные контуры берегов и стран;
- если `Basemap` не установлен, используется встроенная схематичная подложка материков, чтобы карта всё равно не была пустой;
- карта автоматически приближается к области расчёта, поэтому станция, трасса спутника и зона видимости больше не выглядят крошечной точкой на всей карте мира;
- в веб-интерфейсе теперь отдельно показывается статическая карта — именно она попадает в PDF-отчёт.
